# 初步方案设计

---

## 1. 什么情况下触发药品库

调用模型（同决策树），传入患者的本轮发言，为空时取历史对话中最近一轮发言，返回是否需要药品库的 true/false 二元判断

---

## 2. query 设计

之前直接用患者输入做 query 验证的效果不好，改一下，可以先调用模型在返回 true 的时候同时提炼关键词（症状或药品等）出来，再调用 RAG 组提供的 API 以匹配更加精准的信息。

